import XCTest
@testable import AvatarEditLib

final class AvatarEditLibTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AvatarEditLib().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
